#include <bits/stdc++.h>
using namespace std;

int main() {
	int n;
	int a; // 1e6

	cin >> n >> a;

	vector<int> T(n);

	for (int i=0; i<n; i++) {
		cin >> T[i];
	}

	// list to store the time taken to reach the i-th visitor (NOT needed)

	// for (int i=0; i<n; i++) {
		// first customer (i=0)

		// else
			// 2nd: first fin <= second com: return second com + A
			// 2nd: first fin > second com: return first fin + A

		// print i-th

	return 0;
}