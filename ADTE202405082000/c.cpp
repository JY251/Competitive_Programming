#include <iostream>
#include <vector>
#include <string>
#include <unordered_map>

using namespace std;

int main() {
	int n, m;
	cin >> n >> m;

	vector<int> c(n);
	unordered_map<string, int> price;
	int price_others;

	for (int i = 0; i < n; i++) {
		cin >> c[i];
	}
	cin >> price_others;
	for (int i = 0; i < m; i++) {
		
	}


}